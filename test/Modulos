Main
    AVISO = 1
    HORA = 2

    RUTA_AVISO = "/audios/avisos/"
    RUTA_HORA = "/audios/horas/"

    RUTA_AVISO_PROCESADOS = "/baseconocimiento/avisos/"
    RUTA_HORA_PROCESADOS = "/baseconocimiento/horas/"

    inicializar
        LeerAvisoProcesados()
    AprenderAviso()
        LeerDirectorio(Ruta)
        ProcesarArchivo(Tipo)
        GrabarArchivoProcesado()
    ProcesarHora()
        ProcesarArchivo(Tipo)
        Reconocer()

Modules
    LeerDirectorio()
        Toma los archivos en TODO
    ProcesarArchivo(Tipo)
        Obtiene fingerprint, hashname,  inicia el procesamiento INPROGRES
    GrabarArchivoProcesado
        Graba las huellas de memoria a archivos fisico
    LeerAvisoProcesados()
        Lee archivos procesados de memoria y los coloca en memoria
    Reconocer(huellas)
        Compara las huellas de los archivos de
    GenerarXml()
        En base a las coinicidencias Genera archivo xml

